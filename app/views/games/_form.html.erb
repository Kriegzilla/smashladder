<%= form_for @game do |f| %>
  <%= f.label :player_2_id, "Player 2" %>
  <%= f.collection_select :player_2_id, @users, :id, :username, prompt: true %>
  <br>
  <%= f.label :player_1_character_id, "Your Character" %>
  <%= f.collection_select :player_1_character_id, Character.all, :id, :name %>
  <br>
  <%= f.label :player_2_character_id, "Player 2's Character" %>
  <%= f.collection_select :player_2_character_id, Character.all, :id, :name %>
  <br>
  <%= f.label :player_1_stock, "Your Remaining Stock" %>
  <%= f.collection_select :player_1_stock, Game::STOCK, :to_i, :to_s %>
  <br>
  <%= f.label :player_2_stock, "P2 Remaining Stock" %>
  <%= f.collection_select :player_2_stock, Game::STOCK, :to_i, :to_s %>
  <br>
  <%= f.label :stage_id, "Stage" %>
  <%= f.collection_select :stage_id, @stages, :id, :name %>
  <br>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <br>
  <%= f.submit "Submit", class: "button small round expand success" %>
<% end %>
